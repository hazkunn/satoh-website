import type { Metadata, Viewport } from "next";

export const metadata: Metadata = {
  title: "在庫管理 | サトー産業",
  description: "サトー産業 在庫管理システム",
  manifest: "/stock-app-manifest.json",
  appleWebApp: {
    capable: true,
    statusBarStyle: "black-translucent",
    title: "在庫管理",
  },
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: "#1e3a8a",
};

export default function StockAppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="stock-app-layout">{children}</div>
  );
}