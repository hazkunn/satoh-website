# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*

# local stock data (dev only — R2 in production)
/data

# Local photos destined for Cloudflare R2 — NOT server data, never commit.
# These are compressed + uploaded to R2 via scripts/uploadImagesToR2.ts
/R2 Storage cloud. DO NOT PUSH AS SERVER DATA. it's for cloud storage/
